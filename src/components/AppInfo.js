import React from 'react';
import ReactDOM from 'react-dom/client';
import BooksContext from '../context/BooksContext';

const AppInfo = () => {
  return (
    <div className='AppInfo'>
      <h2> What is Calib ? </h2> <p> Basic Info and Additions </p>{' '}
      <p> Tech Specs </p>{' '}
    </div>
  );
};

export default AppInfo;
